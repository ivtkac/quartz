---
created: "2025-11-22"
title: "docker"
tags:
aliases:
---
> [!example] Options
> - `-d` — 
> - `--entrypoint` — 
> - `--env`, `-e`, `--env-file` — 
> - `--init` — 
> - `--interactive`, `-i`, `--tty`, `-t` —
> - `--mount`, `--volume`, `-v` —
> - `--name` —
> - `--network`, `--net` —
> - `--platform` —
> - `--publish`, `-p` —
> - `--restart` —
> - `--rm` — 