---
created: 2025-11-14
title: history
tags:
  - man
  - OS/Linux
  - bash
aliases:
  - Історія команд
---
За замовчуванням [[оболонка]] записує усі команди, які були виконані у ній. Команда `history` є [[bash builtins|вбудованою у bash]] та відображає історію останніх запущених команд.

Історія команд зберігається у файлі `~/.bash_history` після успішного завершення роботи оболонки. До цього моменту вся історія зберігається у тимчасовій пам'яті.

Клавішу "стрілка вгору" можна використовувати для навігації між раніше введеними командами в терміналі, що забезпечує швидкий доступ до попередніх команд.

> [!example] Гарячі клавіші
> - `!!` — повторити останню команду
> - `!number` — повторити елемент списку історії під *number* 
> - `!string` — повторити останній елемент списку історії, що починається з рядка
> - `!?string` — повторити останній елемент списку історії, що містить рядок
> - `CTRL-P`/`CTRL-N` — перехід до попереднього/наступного запису в історії
> - `ALT-<`/`ALT->` — перехід до початку/кінця списку історії
> - `CTRL-R`/`ALT-P` — зворотний інкрементальний/неінкрементальний пошук
> - `ALT-N` — пошук уперед
> - `CTRL-O` — виконання поточного елемента в історії